CREATE TABLE IF NOT EXISTS Users (
                       user_id SERIAL,
                       first_name TEXT,
                       last_name TEXT,
                       email TEXT,
                       password TEXT
);
